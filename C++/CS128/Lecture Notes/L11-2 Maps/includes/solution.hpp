#ifndef SOLUTION_HPP
#define SOLUTION_HPP

#include <map>
#include <string>
#include <vector>
#include <set>

std::map<std::string, std::vector<std::string>> SectionRosters(const std::vector<std::string>& signups, const std::map<std::string, std::string>& assigned);

#endif
